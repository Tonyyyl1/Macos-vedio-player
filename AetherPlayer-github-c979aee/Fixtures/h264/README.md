# H.264 index

H.264 fixtures live in their container directories so the fixture path makes
both dimensions visible. See `../mp4/h264-aac.mp4`, `../mov/h264-aac.mov`, and
`../mkv/h264-aac-srt.mkv`, all listed in `compatibility/manifest.yaml`.
